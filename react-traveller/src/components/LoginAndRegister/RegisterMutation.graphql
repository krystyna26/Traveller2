mutation CreateUser(
  $fist_name: String!
  $last_name: String!
  $email: String!
  $password: String!
) {
  crestUser(
    first_name: $first_name
    last_name: $last_name
    email: $email
    password: $password
  ) {
    first_name
    last_name
    id
  }
}
