import styled, { css } from "styled-components";

const Description = styled.article`
  font-size: rem(14);
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-size: inherit;
    font-weight: 400;
    margin: 0 0 3px 0;
  }
`;

export default Description;
