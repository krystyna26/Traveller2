import React from "react";
import styled, { css } from "styled-components";

const ContainerBlock = styled.section`
  border-radious: rem(4);
  box-shadow: rgba(black, 0.1) 0 rem(4) rem(16)
  margin: 15px
`;

export default ContainerBlock;
